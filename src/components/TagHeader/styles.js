import { StyleSheet } from 'react-native';
import {
    colors as c,
    borders as br,
    fontSizes as fs,
    fontColors as fc
} from '../../styles';

export default StyleSheet.create({
    tagsContainer: {
        flexDirection: 'row',
        height: 56,
        justifyContent: 'flex-start',
        alignItems: 'center'
    }
});
